# Copy to admin/.env.local and edit if you want to override the default
# backend URL (which points at the TensorDock dev server).
#
# Local backend on your laptop:
VITE_API_BASE_URL=http://localhost:8001/api/v1

# A different remote:
# VITE_API_BASE_URL=https://api.yourdomain.com/api/v1
