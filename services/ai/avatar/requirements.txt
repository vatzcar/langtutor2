fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
websockets>=12.0
pyyaml>=6.0.1
# numpy, pillow, imageio-ffmpeg, huggingface_hub are already pinned by
# MuseTalk's own requirements.txt. Don't re-pin — we'd clobber their
# versions and break tensorflow 2.12 (needs numpy<1.24).
